{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:ai-course:gold-s01-private-preview:v1",
  "title": "Gold S01 private preview contracts",
  "type": "object",
  "required": ["request", "host_receipt", "output_manifest", "primary_receipt", "replica_receipt", "restore_receipt", "owner_manifest", "handoff"],
  "properties": {
    "request": {
      "type": "object",
      "required": ["schema_version", "request_id", "profile_id", "scope", "runner", "control", "production", "visual", "timing", "captions", "audio", "execution_authorized", "output_profile", "store_policy", "owner_delivery_class", "restore_verification_required", "no_fake_green"]
    },
    "host_receipt": {
      "type": "object",
      "required": ["schema_version", "profile_id", "versions", "font_manifest_sha256", "primary_probe", "replica_probe", "blockers", "host_lock_status", "receipt_hash", "no_fake_green"]
    },
    "output_manifest": {
      "type": "object",
      "required": ["schema_version", "input_fingerprint", "outputs", "machine_qc_status", "machine_qc_report_sha256", "public_artifacts_created", "private_content_public_exposure", "no_fake_green"]
    },
    "primary_receipt": {"type": "object", "required": ["schema_version", "request_id", "input_fingerprint", "artifacts", "registration_status", "store_class", "receipt_hash", "no_fake_green"]},
    "replica_receipt": {"type": "object", "required": ["schema_version", "request_id", "input_fingerprint", "artifacts", "replica_status", "store_class", "primary_receipt_hash", "receipt_hash", "no_fake_green"]},
    "restore_receipt": {"type": "object", "required": ["schema_version", "request_id", "primary_receipt_hash", "replica_receipt_hash", "restore_status", "verified_artifacts", "cleanup_status", "receipt_hash", "no_fake_green"]},
    "owner_manifest": {"type": "object", "required": ["schema_version", "request_id", "input_fingerprint", "artifacts", "primary_receipt_hash", "replica_receipt_hash", "restore_receipt_hash", "machine_qc_status", "owner_delivery_pointer_class", "human_final_preview_accepted", "allowed_owner_decisions", "manifest_hash", "no_fake_green"]},
    "handoff": {"type": "object", "required": ["schema_version", "execution_request_sha256", "input_fingerprint", "visual_master_receipt_sha256", "ru_preview_receipt_sha256", "primary_registration_receipt_sha256", "replica_receipt_sha256", "restore_receipt_sha256", "station_run_receipt_sha256", "owner_manifest_hash", "human_final_preview_accepted", "receipt_hash", "no_fake_green"]}
  }
}
